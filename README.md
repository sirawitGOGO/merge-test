# Project Setup Guide

## Set Up Dependencies
Install dependencies:
    ```bash
    npm install
    ```

## Executing the Code
Run the application:
    ```bash
    npm start
    ```

## Running Unit Tests
Execute the test:
    ```bash
    npm test
    ```
